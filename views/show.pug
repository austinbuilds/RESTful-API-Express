include ./partials/header

div
  div
    h1 #{music.title}
    h4 #{music.artist}
    h5 #{music.album}
    a(href=music.id+'/edit') edit
    form(action="/"+music.id+"?_method=DELETE" method="POST")
      input(type="submit" value="delete")
  p index

include ./partials/footer
